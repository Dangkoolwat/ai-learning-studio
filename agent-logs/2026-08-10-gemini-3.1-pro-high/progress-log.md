# 작업 로그 (2026-08-10)

## 사이드바 네비게이션 스크롤 추가 및 모바일 레이아웃 최적화

**Status:** 완료
**Files Changed:**
- `assets/css/site.css`

**작업 내용:**
- 데스크탑 환경에서 사이드바 메뉴 항목이 길어질 경우, 오른쪽 메인 페이지 스크롤과 독립적으로 왼쪽 메뉴 영역만 스크롤 가능하도록 수정했습니다.
- 시각적인 기본 스크롤바가 보이지 않도록 처리해 달라는 추가 요청에 따라, 데스크탑 스크롤 기능을 유지하되 스크롤바(`::-webkit-scrollbar` 등)를 숨김 처리했습니다.
- 모바일 환경(@media max-width: 899px)에서는 네비게이션 메뉴가 문서 흐름(static)을 따르므로 고정 스크롤 영역 제한을 해제(max-height: none, overflow-y: visible)하여 모바일 레이아웃이 깨지거나 이중 스크롤이 발생하지 않도록 조치했습니다.

**Verification:**
- 로컬 시스템 기본 환경인 Python 3.9.6에서 구동되도록 프로젝트 전반의 호환성 문제를 해결했습니다.
  - Python 3.10+ 전용 문법인 `@dataclass(slots=True)` 및 `zip(..., strict=True)` 구문을 일괄 제거/수정.
  - `core/build_pipeline.py`의 최소 버전 제한을 3.9로 하향 조정.
- 수정 후 가상환경 없이 기본 터미널 환경에서 `python3 scripts/build.py` 실행 결과, 50개 페이지 빌드가 성공적으로 완료됨을 확인했습니다.

**Handoff Status:** 
- 완료: 가상환경 없이도 빌드가 가능하도록 하위 호환성 수정 완료.
