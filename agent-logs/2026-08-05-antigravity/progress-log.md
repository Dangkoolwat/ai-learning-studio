# 작업 로그 (2026-08-05)

## 1. SNS 프로필 프롬프트 템플릿 최종본 적용
- **개요**: 제안자 피드백을 통해 고도화된 프롬프트 최종본을 수용하여 템플릿 업데이트 작업을 마무리했습니다.
- **수정 사항**:
  - `[MODIFY] pages/sections/image-ai/sns-profile.md`: 
    - 템플릿 전체 텍스트를 제안자 작성 버전으로 완전 교체.

## 2. 핫픽스: 스타일 표현 방식 옵션 칩 누락 현상 수정
- **개요**: 단독으로 한 줄을 차지하는 파란색 칩이 섹션 헤더로 오인되어 렌더링되지 않는 버그를 수정했습니다.
- **수정 사항**:
  - `[MODIFY] pages/sections/image-ai/sns-profile.md`: 옵션 칩 양쪽에 따옴표를 추가(`"[...]"`).

## 3. 출력 개수 지시문 고도화 (AI 방어 로직)
- **개요**: 출력 개수에 대한 AI 서버 리미트 에러를 방지하기 위해 지시문을 세밀하게 다듬었습니다.
- **수정 사항**:
  - `[MODIFY] pages/sections/image-ai/sns-profile.md`: 3단 구성 문장으로 교체 및 칩 라벨 동기화.

## 4. UI 편의성 개선: 사용자 가이드(Tip) 블록 위치 조정 및 핫픽스
- **수정 사항**:
  - `[MODIFY] pages/sections/image-ai/sns-profile.md`: 사용자 안내 팁 최하단 이동 및 마크다운 줄바꿈 렌더링 오류 수정.

## 5. [신규] 이력서 프로필 템플릿 UI 고도화 (정적 프롬프트 도입)
- **개요**: 사용자의 제안에 따라 `resume-profile.md` 프롬프트의 시각적 형태를 최적화하고, 옵션 선택형 인터랙티브 칩(static-prompt)을 적용했습니다.
- **수정 사항**:
  - `[MODIFY] pages/sections/image-ai/resume-profile.md`: 
    - `━━━━━━━━━━━━━━━━━━━━` 형태의 특수문자 디자인을 마크다운 헤딩(`## 1. 사진 용도와 전체적인 인상`)으로 일괄 변환하여 AI의 구조적 인지 능력 극대화.
    - Frontmatter에 `type: static-prompt` 속성을 추가하여, 프롬프트 내부의 `[옵션 / 옵션]` 텍스트들이 동적 드롭다운 칩으로 렌더링되도록 엔진 적용.
- **검증 (사이드 이펙트 체크)**:
  - `page-registry.json`의 기존 라우팅 및 렌더러 설정 충돌 여부 확인 완료 (정상).
  - 프롬프트 내부에 칩 파서(Parser)를 고장 낼 수 있는 단독 줄 배치 브래킷(`[ ]`)이 없음을 확인 (모두 서술형 문장 내에 포함되어 있어 안전함).
  - `python3 scripts/build.py` 정상 빌드 및 28개 페이지 배포 완료. 레이아웃 붕괴나 파싱 에러(Side effect) 없음.
